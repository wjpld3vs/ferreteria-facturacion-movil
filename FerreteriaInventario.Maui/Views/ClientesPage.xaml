<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FerreteriaInventario.Maui.Views.ClientesPage"
             Title="Clientes">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">
            <Frame Style="{StaticResource CardFrameStyle}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Nuevo cliente" FontAttributes="Bold" />
                    <Entry Placeholder="Nombre" Text="{Binding Nombre}" />
                    <Entry Placeholder="Documento" Text="{Binding Documento}" />
                    <Entry Placeholder="Telefono" Text="{Binding Telefono}" />
                    <Entry Placeholder="Correo" Text="{Binding Email}" />
                    <Entry Placeholder="Direccion" Text="{Binding Direccion}" />
                    <Button Text="Registrar cliente" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CreateCommand}" />
                </VerticalStackLayout>
            </Frame>

            <CollectionView ItemsSource="{Binding Clientes}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Style="{StaticResource CardFrameStyle}" Margin="0,0,0,10">
                            <VerticalStackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold" />
                                <Label Text="{Binding Documento}" />
                                <Label Text="{Binding Telefono}" TextColor="{StaticResource TextMuted}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
